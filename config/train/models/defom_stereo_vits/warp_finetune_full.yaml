# defom_stereo_vits / warp_finetune_full
# Fine-tunes the ViT-S full_pretrain run; this produced the released ViT-S model.

includes: [defom_stereo_vits.yaml, ../../ablations/warp_finetune_full.yaml]

optimization:
  batch_size_per_gpu: 4
  learning_rate: 1e-05
